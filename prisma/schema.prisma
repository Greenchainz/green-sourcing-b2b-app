datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// Note: Authentication is handled by Azure Container Apps Easy Auth
// User data is stored in the "Users" table (see database-schemas/schema.sql)
// This Prisma schema is kept minimal for future use
